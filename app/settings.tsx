import Screen from '../components/Screen';

export default function AppSettings() {
  return (
    <Screen
      id="S02  ·  /settings"
      title="앱 설정"
      note="언어, 날짜·거리 형식, 기본 통화, 위치 권한, 알림, 전체 데이터 내보내기/가져오기(JSON)."
    />
  );
}
